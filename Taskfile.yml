# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

tasks:
  build:
    cmds:
      - go build -o output/ .
    sources:
      - ./**/.*go
      - go.mod
    generates:
      - output/hass-telegram-bot
    silent: false

  lint:
    cmds:
      - golangci-lint run
    silent: false
